global:
  scrape_interval: 3s       # fast scrape to capture per-job worker-node metrics
  evaluation_interval: 15s

scrape_configs:
  - job_name: "stoomboot"
    static_configs:
      - targets: ["localhost:9118"]   # adjust if exporter runs on another host/port
